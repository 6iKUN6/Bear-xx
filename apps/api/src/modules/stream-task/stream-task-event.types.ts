/**
 * 流式任务事件类型
 *
 * 定义已收敛到共享包 @litter-bear/types/protocol（前后端唯一事实源）。
 * 本文件仅做 re-export，保持既有相对路径 import 不变。
 */
export {
  StreamTaskEventType,
  STREAM_TASK_TERMINAL_EVENT_TYPES,
} from '@litter-bear/types/protocol';
